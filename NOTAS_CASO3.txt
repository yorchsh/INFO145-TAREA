1. Cosas importantes:

- main.cpp
     Seccion del Caso 3:
      * los include de shannon_fano.hpp y csv.hpp
      * el bloque "CASE 3" dentro del benchmark (construye, busca y mide). La
        lineal usa lineal_vector (linealVector, eps=256), que ahora se declara
        arriba con los otros vectores y se genera al INICIO del benchmark; la
        normal la comparte con Casos 1 y 2 (normal_vector)
      * la escritura de salida.csv (SOLO las filas con caso=3, ver punto 2)

    Casos 1 y 2 están tal cual estaban

2. Cosas del Caso 3 que coordinar:

- CSV de salida:
  Hay que agregar las filas de Caso 1 y Caso 2. subí el ofstream ya que
  si cada uno lo abre por su lado, se pisan el contenido.
  Columnas que usa:
    tamano,distribucion,caso,construccion_ms,busqueda_ms,n_busquedas,
    espacio_bits,espacio_explicito_bits

- Benchmark: el benchmark actual usa millones de elementos.
  El Caso 3 arma una tabla (map) con los gaps distintos; con datos de gaps
  chicos está bien, pero conviene revisar la escala/memoria al integrar.

- Aparte, para el benchmark, sigue faltando una funcion que mida N busquedas
  llamando a ShannonFano::buscar, tipo el stub gapBinSearch del Caso 2.

- README: falta documentar todo
