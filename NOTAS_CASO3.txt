1. Archivos Nuevos

- include/shannon_fano.hpp
    El algoritmo del Caso 3. Clase caso3::ShannonFano<T>:
      * constructor(vector<T>, saltos): ordena, calcula gaps, los comprime
        con Shannon-Fano y arma el sample.
      * buscar(x): devuelve la posicion en el arreglo ordenado, o -1.
      * espacio_bits(): cuanto ocupa la estructura comprimida + el sample.

- caso3.py
    Prototipo en Python


2. Archivos Modificados

- include/csv.hpp
    Las funciones csv::read y csv::write estaban a medio escribir (no
  compilaban: faltaba std::, faltaba el template en write, read no
  retornaba nada). Está completa. Ahora:
      * csv::read<T>(ruta): lee enteros separados por coma o salto de linea
      * csv::write<T>(ruta, vector): los escribe separados por coma

- include/vector_generation.hpp
    Solo se agregó la funcion vec_gen::linealVector(size, epsilon), que genera
  el arreglo lineal del enunciado (A[i] = A[i-1] + rand() % epsilon, gaps
  acotados). uniformVector y normalVector quedaron como estaban

- main.cpp
     Seccion del Caso 3:
      * los include de shannon_fano.hpp y csv.hpp
      * el bloque "CASE 3" dentro del benchmark (construye, busca y mide). Para
        la lineal genera su propio lineal_vector (linealVector, eps=256); la
        normal la comparte con Casos 1 y 2 (normal_vector)
      * la escritura de salida.csv (SOLO las filas con caso=3, ver punto 4)
      * el modo archivo: rama "argc == 3 && -i" que lee un csv, construye la
        estructura y busca de forma interactiva (reporta posicion + tiempo)
    Casos 1 y 2 están tal cual estaban

3. main.cpp actualmente no compila

Caso 3 se compiló en un archivo aparte con funciones distintas, no
causa el impedimiento de compilar.
Es por cosas pendientes de los Casos 1 y 2:

  a) include/gap_coding.hpp (Caso 2) tiene errores y no compila ni al incluirlo.

  b) main.cpp línea 88 (Caso 1): dice         *******[ESTO SI LO CAMBIÉ]*******
  std::int64_t normal_vector = vec_gen::normalVector<...>(...);
  Eso re-declara normal_vector como un solo numero. Debería ser:
  normal_vector = vec_gen::normalVector<...>(...); [ *** POR REVISAR *** ]
  (reusar el vector de arriba, como hace uniform_vector en la línea 63).

4. Cosas del Caso 3 que coordinar:

- CSV de salida: bloque del Caso 3 abre
      std::ofstream salida("salida.csv");
  DENTRO de la seccion Caso 3 y escribe solo las filas con caso=3.
  Hay que agregar las filas de Caso 1 y Caso 2. Lo ideal es SUBIR ese
  ofstream al inicio del benchmark para que los 3 casos escriban en el
  mismo archivo (si cada uno lo abre por su lado, se pisan el contenido).
  Columnas que usa:
    tamano,distribucion,caso,construccion_ms,busqueda_ms,n_busquedas,
    espacio_bits,espacio_explicito_bits

- MISMO vector para los 3 casos: los 3 deberian medirse sobre el MISMO arreglo.
  AHORA: el Caso 3 ya usa vec_gen::linealVector (genera su PROPIO lineal_vector
  en su bloque, eps=256), porque con uniformVector (rango completo) los gaps no
  se repiten y no comprime nada. Pero Casos 1 y 2 siguen usando uniform_vector.
  => Falta que el Caso 1 cambie su generacion (linea 63) de uniformVector a
     linealVector y comparta ese vector, asi los 3 miden la MISMA data lineal.
  (La normal sí la comparten los 3, esa parte esta ok.)

- Benchmark: el benchmark actual usa millones de elementos.
  El Caso 3 arma una tabla (map) con los gaps distintos; con datos de gaps
  chicos está bien, pero conviene revisar la escala/memoria al integrar.

- Gaps desde el Caso 2: actualmente el Caso 3 RECALCULA los gaps por su cuenta (para
  no depender del Caso 2). Cuando GapArray funcione, se podrian leer los
  gaps de ahi (con un get(i) que devuelva el valor) en vez de volver a restar.
  Esta anotado en shannon_fano.hpp (en el constructor).
  *** IMPORTANTE ***: leerlos del GapArray obliga a decodificar su ancho fijo solo para
  re-codificarlos con Shannon-Fano, lo cual es redundante. Pero así lo
  pide el PDF (el Caso 3 comprime el arreglo del Caso 2), asi que igual va.
  Aparte, el Caso 3 igual tiene que armar SU propio empaquetado de bits y SU
  propio sample de posiciones: los offsets del GapArray son de ancho fijo y no
  sirven para el bitstream de largo variable de Shannon-Fano.

- binary_search NO se puede usar directo para el Caso 3. Sus funciones
  (trueRandom / selectRandom) hacen std::binary_search sobre un vector plano,
  que es el Caso 1. El Caso 3 busca sobre su sample + decodificando, no hay un
  vector plano que pasarle. A lo más: completar el stub gapBinSearch(GapArray,..)
  que ya esta ahí, y hacer uno parecido para el Caso 3 (mismo patron de medir N
  busquedas pero llamando a ShannonFano::buscar).

- README: falta documentar todo

5. Se probó el Caso 3 aparte:

Compila, enlaza y corre con g++ 15 (MSYS2 ucrt64). Resultados de ejemplo:
  - distribucion lineal: aprox. 6x menos espacio que la version explicita.
  - distribucion normal: aprox 12x menos espacio.
  - busquedas devuelven la posicion correcta; valores ausentes dan -1.


Notas para mí mismo:

- Decidir si dejar el cálculo de gaps en Shannon-Fano o pasarlo a gap_coding.hpp (en el PDF
  está implícito el uso de los gaps obtenidos en Caso 2, pero causaría que tengamos que
  re-codificarlos con Shannon-Fano, y no tiene sentido)

- Binary Search tampoco puede ser reutilizado, mismo problema que gap_coding:
  ver si lo dejo en Shannon-Fano o lo re-implemento dentro de binary_search.hpp