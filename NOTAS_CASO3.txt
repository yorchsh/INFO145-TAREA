1. Cosas importantes:

- main.cpp
     Seccion del Caso 3:
      * los include de shannon_fano.hpp y csv.hpp
      * el bloque "CASE 3" dentro del benchmark (construye, busca y mide). La
        lineal usa lineal_vector (linealVector, eps=256), que ahora se declara
        arriba con los otros vectores y se genera al INICIO del benchmark; la
        normal la comparte con Casos 1 y 2 (normal_vector)
      * la escritura de salida.csv (SOLO las filas con caso=3, ver punto 2)

    Casos 1 y 2 están tal cual estaban

2. Cosas del Caso 3 que coordinar:

- CSV de salida

- Benchmark: el benchmark actual usa millones de elementos.
  El Caso 3 arma una tabla (map) con los gaps distintos; con datos de gaps
  chicos está bien, pero conviene revisar la escala/memoria al integrar. [Por revisar]

- Aparte, para el benchmark, sigue faltando una funcion que mida N busquedas
llamando a ShannonFano::buscar, tipo el stub gapBinSearch del Caso 2.

- Falta ShannonFano crea el gap_array de nuevo, shannonFano deberia ser construido con el gap_array pasado como paremetro.

[Notas para mí (creo)]
1. Modo -i solo busca en Caso 3. El PDF pide que el usuario elija "en qué estructura" (Caso 1/2/3). Falta cuando Caso 2 esté listo.
2.Benchmark corre UN solo tamaño. El PDF (metodología) pide tamaños incrementales (10^6, 10^7...) necesario para los gráficos del Hito 2.
3. README es plantilla: falta documentar modos, rango de números (int64 / strtoll) y salida. Es requisito explícito del Hito 1.
4. (Latente) GapArray sin constructor de copia (regla de 3) riesgo de doble-free si se copia.
5. El benchmark corre completo (Caso 1, 2, 3) y la compresión lineal funciona (601024 vs 3200000 bits). Pero el modo -i no imprimió nada
El -i construye ShannonFano(datos) sobre las 20 filas de entrada.csv.

